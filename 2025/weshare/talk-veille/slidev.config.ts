import { defineConfig } from '@slidev/types'

export default defineConfig({
  // Canvas plus grand pour un meilleur rendu sur les écrans modernes
  // Par défaut: 980px, augmenté à 1280px
  canvasWidth: 1280,
})
